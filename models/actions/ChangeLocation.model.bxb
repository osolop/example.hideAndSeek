action (ChangeLocation) {
  type (Fetch)
  description (Move to other location)
  collect {
    input (location) {
      type (Location)
      min (Required) max (One)
    }
  }
  output (MoveResultConcept)
}
